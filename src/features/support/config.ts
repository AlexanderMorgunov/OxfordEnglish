/**
 * Donation payment link (ЮKassa payment-link or another ruble rail).
 * Paste the URL here once the ЮKassa account is set up. While empty, the Support
 * page shows a friendly "coming soon" state instead of a live button.
 *
 * Payments must live on the PWA, never inside the Telegram Mini App (Telegram ToS),
 * so this is always an external link that opens in the browser.
 */
export const SUPPORT_URL = 'https://pay.cloudtips.ru/p/4592441c';
