{
  "name": "dayenglish-api",
  "private": true,
  "type": "module",
  "description": "DayEnglish backend v1 — accounts + cross-device sync (Hono on Yandex Serverless Containers). Skeleton: auth only (slice 1). See ../docs/backend-v1-design.md.",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@hono/node-server": "^1.13.0",
    "hono": "^4.6.0",
    "jose": "^5.9.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
